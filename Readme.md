```markdown
# ⚡1. TypeScript-এ interface VS Type Alias: মূল পার্থক্যগুলো

> TypeScript-এর দুটি গুরুত্বপূর্ণ ফিচার: Interface এবং Type Alias-এর মধ্যে প্রধান পার্থক্যগুলো নিচে আলোচনা করছি।
 শেখা আরও সহজ করার জন্য তৈরি করা।

---

## 🌟 ১. সাধারণ টাইপ (Primitive, Union, Tuple)

Type Alias যেকোনো ধরনের টাইপের জন্য নতুন নাম তৈরি করতে পারে। অন্যদিকে, Interface শুধু **object-এর গঠন** বর্ণনা করতে পারে।

#### কোড উদাহরণ

```typescript
// ✅ Type Alias দিয়ে সব করা যায়
type Address = string;
type Status = 'active' | 'inactive';
type Coordinates = [number, number];

// ❌ Interface দিয়ে এগুলো করা যায় না
```

---

## 🤝 ২. Declaration Merging

একই নামের একাধিক Interface স্বয়ংক্রিয়ভাবে একসাথে যুক্ত হয়ে যায়। Type Alias-এ এই সুবিধা নেই।

#### কোড উদাহরণ

```typescript
// প্রথম ঘোষণা
interface UserProfile {
  name: string;
}

// দ্বিতীয় ঘোষণা
interface UserProfile {
  age: number;
}

// ফলাফল: দুটি property একসাথে এসেছে
const user: UserProfile = {
  name: 'Shuvo',
  age: 28
}

// 📌 মনে রাখবেন: Type Alias একই নামে একাধিকবার লেখা যায় না।
```

---

## 🔗 ৩. Extending এবং Combining

দুটো টাইপ-ই object extend করতে পারে, তবে ভিন্ন পদ্ধতিতে।

#### কোড উদাহরণ

```typescript
// Interface Extending
interface Shape {
  color: string;
}

interface Circle extends Shape {  // extends দিয়ে বাড়ানো
  radius: number;
}

// ------------------------------------

// Type Alias Intersection
type ShapeType = {
  color: string;
};

type SquareType = ShapeType & {  // & (and) দিয়ে যুক্ত করা
  sideLength: number;
};
```

---

## ⚙️ ৪. Advanced Types

জটিল লজিক (যেমন Conditional Types) ব্যবহারের জন্য Type Alias জরুরি।

#### কোড উদাহরণ

```typescript
// জটিল লজিক তৈরির জন্য Type Alias শক্তিশালী
type Check<T> = T extends string ? 'IsString' : 'IsNotString';

type Result1 = Check<"Hello">;  // 'IsString'
type Result2 = Check<number>;   // 'IsNotString'

// 💡 এই ধরনের লজিক Interface দিয়ে তৈরি করা সম্ভব নয়।
```

---

## 🎯 কখন কোনটি ব্যবহার করবেন?

| আপনার প্রয়োজন | ব্যবহার করুন | কারণ |
| :--- | :--- | :--- |
| একটি object-এর সাধারণ গঠন | **`interface`** | পরিষ্কার এবং Declaration Merging-এর সুবিধা |
| Union/Tuple/Primitive Type তৈরি | **`type`** | Interface এগুলো করতে পারে না |
| বিদ্যমান লাইব্রেরি বাড়ানো | **`interface`** | Merging-এর জন্য আবশ্যক |
| জটিল/শর্তাধীন টাইপ লজিক | **`type`** | Advanced Type ফিচারের জন্য প্রয়োজন |

---

## 📚 শেষ কথা

সাধারণ object-এর কাজে **Interface** ব্যবহার করুন, আর জটিল বা বিশেষ টাইপ তৈরিতে **Type Alias** ব্যবহার করুন।

---



# 🎨2. any, unknown, এবং never Types: পার্থক্য কী?

TypeScript-এ তিনটি বিশেষ টাইপ আছে যেগুলো একটু আলাদা কাজ করে। চলুন দেখি।

---

## 🔴 any Type - সবকিছু চলবে!

`any` হলো TypeScript-এর টাইপ চেকিং বন্ধ করে দেওয়ার মতো। যা খুশি করুন, কোনো error দেবে না।

#### কোড উদাহরণ

```typescript
let data: any = "Hello";
data = 42;              // ✅ চলবে
data = true;            // ✅ চলবে
data.nonExistent();     // ✅ compile হবে (কিন্তু runtime-এ error!)

// 📌 সমস্যা: টাইপের কোনো নিরাপত্তা নেই
```

### কখন ব্যবহার করবেন?

- পুরনো JavaScript কোড নিয়ে আসার সময়
- দ্রুত prototype তৈরি করতে

⚠️ **সতর্কতা**: `any` বেশি ব্যবহার করলে TypeScript-এর সুবিধা হারায়!

---

## 🟡 unknown Type - নিরাপদ any

`unknown` হলো `any`-এর নিরাপদ ভার্সন। ব্যবহারের আগে টাইপ চেক করতে হয়।

#### কোড উদাহরণ

```typescript
let userInput: unknown;

userInput = "Hello";
userInput = 42;  // ✅ সব ধরনের value রাখা যায়

// ❌ সরাসরি ব্যবহার করা যাবে না
// userInput.toUpperCase(); // Error!

// ✅ টাইপ চেক করে তারপর ব্যবহার
if (typeof userInput === "string") {
  console.log(userInput.toUpperCase()); // এখন চলবে
}
```

### কখন ব্যবহার করবেন?

- API থেকে আসা ডেটা
- User input নিয়ে কাজ করার সময়

✅ **সুবিধা**: `any`-এর চেয়ে অনেক বেশি নিরাপদ!

---

## 🟣 never Type - কখনো কিছু হবে না

`never` বলে "এখানে কখনোই কোনো মান থাকবে না।"

#### কোড উদাহরণ

```typescript
// Function যেটা কখনো return করে না
function throwError(message: string): never {
  throw new Error(message);
}

// Infinite loop
function infiniteLoop(): never {
  while (true) {
    // চিরকাল চলবে
  }
}

// Exhaustive checking
type Shape = "circle" | "square";

function getArea(shape: Shape) {
  switch (shape) {
    case "circle":
      return Math.PI * 10 * 10;
    case "square":
      return 10 * 10;
    default:
      const exhaustiveCheck: never = shape; // এখানে কখনো আসবে না
      return exhaustiveCheck;
  }
}
```

### কখন ব্যবহার করবেন?

- Function যেটা সবসময় error throw করে
- Exhaustive type checking করার জন্য

---

## 📊 তিনটির তুলনা এক নজরে

| বিষয় | any | unknown | never |
| :--- | :--- | :--- | :--- |
| **যেকোনো মান দেওয়া যায়?** | ✅ হ্যাঁ | ✅ হ্যাঁ | ❌ না |
| **টাইপ চেক করা লাগে?** | ❌ না | ✅ হ্যাঁ | - |
| **নিরাপদ?** | ❌ না | ✅ হ্যাঁ | ✅ হ্যাঁ |
| **ব্যবহারের ক্ষেত্র** | legacy কোড | API response | Error function |

---

## 🎯 সহজ মনে রাখার উপায়

```typescript
// any = "যা খুশি তাই করো"
let a: any = 123;
a.doAnything(); // কোনো সমস্যা নেই (কিন্তু বিপজ্জনক!)

// unknown = "আগে চেক করো"
let b: unknown = 123;
if (typeof b === "number") {
  b.toFixed(2); // এখন চলবে
}

// never = "এখানে কিছু নেই"
function c(): never {
  throw new Error("Always fails");
}
```

---

## ✨ সেরা পরামর্শ

1. **any এড়িয়ে চলুন** - যদি না একদম প্রয়োজন হয়
2. **unknown ব্যবহার করুন** - যখন টাইপ জানা নেই
3. **never ব্যবহার করুন** - অসম্ভব অবস্থা বোঝাতে

মনে রাখবেন: `any` → সব চলবে, `unknown` → চেক করে নাও, `never` → কিছুই না! 🚀

---



<div align="center">

## 👨‍💻 Created By

**Mehedi Hasan Akash**

---

📝 **License**: MIT | 💙 Made with TypeScript

</div>

```

