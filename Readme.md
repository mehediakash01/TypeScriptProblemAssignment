

```markdown
# ⚡ TypeScript-এ interface VS Type Alias: মূল পার্থক্যগুলো

> TypeScript-এর দুটি গুরুত্বপূর্ণ ফিচার: Interface এবং Type Alias-এর মধ্যে প্রধান পার্থক্যগুলো নিচে আলোচনা করছি। শেখা আরও সহজ করার জন্য তৈরি করা।

---

## 🌟 ১. সাধারণ টাইপ (Primitive, Union, Tuple)

Type Alias যেকোনো ধরনের টাইপের জন্য একটি নতুন নাম (`alias`) তৈরি করতে পারে। অন্যদিকে, Interface শুধুমাত্র **object-এর গঠন** বর্ণনা করতে পারে।

### Type Alias-এর সুবিধা

| প্রকার | উদাহরণ |
| :--- | :--- |
| **Primitive Types** | `type Address = string;` |
| **Union Types** | `type Status = 'active' \| 'inactive';` |
| **Tuple Types** | `type Coordinates = [number, number];` |

#### কোড উদাহরণ (Example: Union Type)

```typescript
// ✅ Type Alias দিয়ে এটি করা যায়
type ErrorCode = string | number;

// ❌ Interface দিয়ে এটি করা যায় না (Error দেবে)
// interface ErrorCode = string | number;
```

---

## 🤝 ২. Declaration Merging

Declaration Merging হলো Interface-এর একটি **বিশেষ ক্ষমতা**। একই নামের একাধিক Interface স্বয়ংক্রিয়ভাবে একসাথে যুক্ত হয়ে একটি Interface তৈরি করে। Type Alias-এ এই সুবিধা নেই।

### Interface Merging

| Interface A | Interface B | যুক্ত হওয়ার পর |
| :--- | :--- | :--- |
| `name: string;` | `age: number;` | `name: string; age: number;` |

#### কোড উদাহরণ

```typescript
// প্রথম ঘোষণা (Interface A)
interface UserProfile {
  name: string;
}

// দ্বিতীয় ঘোষণা (Interface B)
interface UserProfile {
  age: number;
}

// ফলাফল: দুটি property একটি UserProfile-এ একসাথে চলে এসেছে
const user: UserProfile = {
  name: 'Shuvo',
  age: 28
}

// 📌 মনে রাখবেন: Type Alias একই নামে একাধিকবার লেখা যায় না।
```

---

## 🔗 ৩. Extending এবং Combining

দুটো টাইপ-ই object extend করতে পারে, তবে ভিন্ন পদ্ধতিতে।

| কাজের ধরন | Interface (`interface`) | Type Alias (`type`) |
| :--- | :--- | :--- |
| **বাড়ানো (Extending)** | `extends` কিওয়ার্ড ব্যবহার করে (Inheritance) | Intersection Type (`&`) ব্যবহার করে (Composition) |

#### কোড উদাহরণ

```typescript
// Interface Extending
interface Shape {
  color: string;
}

interface Circle extends Shape {  // extends দিয়ে বাড়ানো
  radius: number;
}

// ------------------------------------

// Type Alias Intersection
type ShapeType = {
  color: string;
};

type SquareType = ShapeType & {  // & (and) দিয়ে যুক্ত করা
  sideLength: number;
};
```

---

## ⚙️ ৪. Advanced Types এবং Function Types

Function Type বর্ণনা করার জন্য দুটোই ব্যবহার করা যায়, তবে জটিল লজিক (যেমন Conditional Types) ব্যবহারের জন্য Type Alias অত্যন্ত জরুরি।

### Conditional Type

```typescript
// জটিল লজিক তৈরির জন্য Type Alias শক্তিশালী
type Check<T> = T extends string ? 'IsString' : 'IsNotString';

type Result1 = Check<"Hello">;  // 'IsString'
type Result2 = Check<number>;   // 'IsNotString'

// 💡 এই ধরনের লজিক Interface দিয়ে তৈরি করা সম্ভব নয়।
```

---

## 🎯 কখন কোনটি ব্যবহার করবেন? (দ্রুত সিদ্ধান্ত)

| আপনার প্রয়োজন | ব্যবহার করুন | কারণ |
| :--- | :--- | :--- |
| একটি object-এর **সাধারণ গঠন** | **`interface`** | পরিষ্কার সিনট্যাক্স এবং Declaration Merging-এর সুবিধা। |
| **Union/Tuple/Primitive Type**-এর alias তৈরি | **`type`** | Interface এই কাজগুলো করতে পারে না। |
| **বিদ্যমান লাইব্রেরি বাড়ানো** | **`interface`** | Merging-এর জন্য এটি আবশ্যক। |
| **জটিল/শর্তাধীন/ম্যাপিং টাইপ** লজিক তৈরি | **`type`** | Advanced Type ফিচারের জন্য Type Alias প্রয়োজন। |

---

## 📚 শেষ কথা

সাধারণ object-এর কাজে **Interface** ব্যবহার করুন, আর জটিল বা বিশেষ টাইপ তৈরিতে **Type Alias** ব্যবহার করুন। দুটোরই নিজস্ব জায়গা আছে!

---

**Created By**: Mehedi Hasan Akash  

```

